import { BoldeningPipePipe } from './boldening-pipe.pipe';

describe('BoldeningPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BoldeningPipePipe();
    expect(pipe).toBeTruthy();
  });
});
